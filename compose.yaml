services:
  web:
    image: my-docker-webapp
    container_name: compose-web
    ports:
      - "8082:80"
    volumes:
      - my-web-volume:/usr/share/nginx/html
    networks:
      - my-network

volumes:
  my-web-volume:

networks:
  my-network:
    driver: bridge
